import 'package:flutter/material.dart';

import '../card.dart';

class FreeTab extends StatelessWidget {
  const FreeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return NftCard(
      imagePath: 'assets/images/apiens_2.png',
    );
  }
}
